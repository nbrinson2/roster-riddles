import type { FirebaseOptions } from 'firebase/app';

/**
 * Copy to firebase.local.ts (created automatically on npm install if missing)
 * and paste your Firebase web app config from the Firebase console.
 */
export const firebaseConfig: FirebaseOptions = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
  measurementId: '',
};
