HELP.md

# Generated for CI / Docker builds (see scripts/generate-env-prod.mjs)
src/environment.prod.ts
src/environment.staging.ts

target/
.angular/cache
dist/
node_modules/

# Local env / secrets (never commit API keys or service accounts)
.env
.env.*
!.env.example

# Service account JSON (e.g. Firebase Admin key for roster-riddles-staging); keep `secrets/.gitkeep` only
secrets/**
!secrets/.gitkeep

# Python (daily-job scripts, one-off uploads)
.venv/
venv/
.venv-upload/
__pycache__/
*.py[cod]

# Logs & Firebase local debug
*.log
npm-debug.log*
firebase-debug.log*
firestore-debug.log

# Test output
coverage/

# Story G1 — local leaderboard cohort fixture (copy from docs/fixtures example)
leaderboard-test-cohort.json

# Story G1 — weekly contest staging seed (copy from docs/fixtures example)
weekly-contest-staging.json

# OS
.DS_Store
Thumbs.db

### VS Code ###
.vscode/

### JetBrains ###
.idea/

